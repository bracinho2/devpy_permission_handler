enum AppPermissionStatus {
  denied,
  granted,
  restricted,
  permanentlyDenied,
}

enum AppPermissions {
  camera,
  location,
}

enum Os {
  web,
  android,
  ios,
  macOS,
  linux,
  windows,
  fuchsia,
}
